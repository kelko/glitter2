global:
  globalVal:
    value: "Set in d.glitter"

injection:
  - rendered:
      render: "b.glitter"

#Result:
#> The value of `globalVal` is: Set in d.glitter
#> The value of `globalVal` is: Set in d.glitter

template: |
  *> rendered
